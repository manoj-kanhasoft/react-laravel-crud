// Exporting the Stripe API key stored in the environment variable REACT_APP_STRIPE_KEY
export const STRIPE_KEY = process.env.REACT_APP_STRIPE_KEY;

// Exporting the site URL stored in the environment variable REACT_APP_SITE_URL
export const SITE_URL = process.env.REACT_APP_SITE_URL;

// Exporting the API URL stored in the environment variable REACT_APP_API_URL
export const API_URL = process.env.REACT_APP_API_URL;
